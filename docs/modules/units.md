# Modulo units

## Proposito

Administra unidades de medida de producto.

## Endpoints

- `GET /api/units`
- `POST /api/units`
- `PUT /api/units/:id`
- `DELETE /api/units/:id`

## Campos inferidos

- `name`
- `abbreviation`
- `is_active`
