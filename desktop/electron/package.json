{
  "name": "@soft-manustienda-platform/desktop-electron",
  "version": "0.1.0",
  "private": true,
  "description": "Manus POS Electron online desktop shell",
  "main": "dist/main.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "electron": "^42.4.1",
    "typescript": "^6.0.3"
  }
}
