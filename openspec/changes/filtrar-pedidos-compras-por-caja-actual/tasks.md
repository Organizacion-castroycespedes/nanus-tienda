# Tasks

- [x] Crear migracion aditiva para `orders.cash_session_id` y `purchases.cash_session_id`.
- [x] Guardar `cash_session_id` en nuevos pedidos.
- [x] Guardar `cash_session_id` en nuevas compras.
- [x] Agregar `cashScope=current|all` en `GET /api/orders`.
- [x] Agregar `cashScope=current|all` en `GET /api/purchases`.
- [x] Bloquear `cashScope=all` para usuarios operativos.
- [x] Ajustar frontend de Pedidos para scope caja actual por defecto.
- [x] Ajustar frontend de Compras para scope caja actual por defecto.
- [x] Crear evidencia QA.
- [x] Ejecutar validaciones.
